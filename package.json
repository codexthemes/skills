{
  "name": "@codexthemes/skills",
  "version": "0.1.0",
  "private": true,
  "description": "Official agent skills for creating and validating Codex desktop themes.",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "validate": "tsx skills/codex-themes-creator/scripts/validate-skill.ts skills/codex-themes-creator",
    "test": "tsx skills/codex-themes-creator/scripts/self-test.ts",
    "check": "npm run typecheck && npm run validate && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  }
}
